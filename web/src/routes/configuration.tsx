import Page from '~/components/page';


export const path = '/configuration';
export const element = Configuration;

function Configuration() {
	return <Page>
		<span>Possibly a page for easier configuration.</span>
	</Page>;
}